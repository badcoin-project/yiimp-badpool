#!/bin/sh
# Read-only preview only. Wallet approval and every accounting mutation remain separate.
set -eu

repo_dir=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
cd "$repo_dir/web"
exec php yaamp/yiic.php badpoolguard batch-run-preview "$@"
